# Date

Una utilidad para obtener la fecha en formato timestamp y long time

# Instalación

```bash
npm install publish-date-npm
```

# Uso

const dateUtils = require('publish-date-npm ');

console.log(dateUtils.getTimestamp());
console.log(dateUtils.getLongTime());
